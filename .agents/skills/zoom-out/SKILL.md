---
name: zoom-out
description: >-
  在陷入复杂细节排查、长时间调试未果、或对局部逻辑感到混乱时使用。
  跳出单行代码与散乱文件，拉升到系统宏观视角，全景复盘数据流、协议拓扑与业务主线。
---

# Zoom-Out 全局宏观复盘技能

## 何时使用
- 某个并发或长连接网络 Bug 调试了多轮依然偶现；
- 感觉修改此处的代码可能会引发其他多处隐性崩溃；
- 用户感觉方向走偏，说：“我们停下来理一理”、“感觉越改越乱”。

## 执行流程
1. **停止局部微调**：
   - 立即暂停修改局部的具体语句或修修补补；
2. **绘制端到端全链路图**：
   - 梳理真实请求的完整拓扑：客户端 WebSocket ➔ Netty Channel ➔ Gateway 包装 ➔ 消息路由分发 (`MsgRouter`) ➔ Handler 处理 ➔ Manager 内存状态流转 ➔ SQLite 异步或同步持久化 ➔ 回包路径；
3. **核查三个核心不变式（Invariants）**：
   - **会话状态生命周期**：用户登录态和 Channel 的绑定在何处建立？在何处注销？重连时如何抢占？
   - **房间/桌子归属权**：桌子对象由哪个 Manager 独占管理？是否存在内存双份副本？
   - **异常隔离边界**：发生异常时是熔断局部连接，还是会拖垮整个 EventLoop 线程？
4. **输出复盘总结与重定位建议**：
   - 说明当前死锁或卡壳的根因所在哪一层，提出跳出死胡同的最短修复路径。
