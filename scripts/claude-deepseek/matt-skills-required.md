<!-- matt-skills-required:start -->
# 必用工程技能

- 想需求、对齐思路时，必须使用 `/grill-with-docs`。
- 准备动手实现时，必须使用 `/caveman`。
- 遇到复杂 Bug 时，必须使用 `/diagnose`。
- 写核心纯函数或底层业务逻辑时，必须使用 `/tdd`。
<!-- matt-skills-required:end -->
